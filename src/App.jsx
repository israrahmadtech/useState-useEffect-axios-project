import CardList from './Components/CardList'

function App() {

  return (
    <>
      {/* { loading && <h2>Loading... <div className="loader"></div></h2> }
      { users?.map((user, index) => <h1 key={index}>{user.name}</h1>) }
      { error && <h2>Something went wrong!</h2> }
      <button onClick={"url"}>Users</button> */}
      <CardList/>
    </>
  )
}

export default App